# WHAM Quick Replies — стабилизация beta

## Канонический продукт

WHAM — локальное Windows 10/11 приложение только для текстовых макросов:

`macros.json` → глобальная горячая клавиша → вставка Unicode-текста в активное поле.

В beta входят только:

- локальный список текстовых макросов;
- название, текст и горячая клавиша каждого макроса;
- глобальные сочетания клавиш;
- вставка текста в активное поле Windows;
- системный трей;
- локальная установка, запуск, автозапуск, удаление и диагностика;
- сохранность пользовательского `macros.json` при обновлении и переустановке.

OCR, AI, облако, CRM, аналитика, SEO, переменные шаблонов и остальные модули остановлены.

## Ветки и сохранность наработок

- `main` — только подтверждённая рабочая версия.
- `stabilize/quick-replies-core-2026-08-04` — активная ветка стабилизации beta.
- Черновой PR: `#2`.
- Нестабильные решения разрешено откатывать или упрощать.
- Наработки не удаляются безвозвратно: их сохраняют Git-история, отдельная ветка или архивный коммит.
- Автоматические workflow не должны переписывать исходный код приложения. CI только проверяет и собирает пакет.

## Текущий технический статус

- [x] Минимальное PowerShell 5.1/WinForms ядро без обязательного self-test при обычном запуске.
- [x] Глобальные горячие клавиши регистрируются через отдельное скрытое окно.
- [x] Self-test не занимает реальную глобальную комбинацию.
- [x] Старые `macros.json` без `id` мигрируются с резервной копией.
- [x] Повреждённый файл сохраняется как `.bak` перед восстановлением defaults.
- [x] Второй экземпляр блокируется локальным mutex.
- [x] Устаревший workflow, переписывавший ядро, удалён в стабилизационной ветке.
- [x] Контрактные тесты приведены к фактическому минимальному beta-ядру.
- [ ] Получить успешный Windows CI для PR #2.
- [ ] Подтвердить ручную вставку в Блокнот и браузер.
- [ ] Подтвердить сохранность пользовательских макросов после перезапуска и обновления.

## Критерий beta

1. Установка или portable-запуск выполняется без прав администратора.
2. Значок WHAM появляется в системном трее.
3. `Ctrl+Alt+1` — `Ctrl+Alt+5` вызывают соответствующие макросы.
4. Русский многострочный текст вставляется в Блокнот и браузер.
5. Изменение `macros.json` и команда **Перезагрузить макросы** активируют новый текст и сочетание.
6. Перезапуск и обновление не удаляют пользовательский файл макросов.
7. Ошибка занятой клавиши или запуска отображается пользователю и записывается в `WHAM-errors.log`.

До выполнения этих пунктов новые функции не добавляются, а PR #2 не сливается в `main`.
